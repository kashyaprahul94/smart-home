import { NgModule } from "@angular/core";

import { HTTPClient, RESTClient } from "./networking";

@NgModule({
	imports: [

	],
	declarations: [

	],
	providers: [
		HTTPClient,
		RESTClient,
	],
	schemas: [

	],
	exports: [

	],
})
export class IonicCommonModule {

}